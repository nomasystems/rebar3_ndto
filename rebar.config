{erl_opts, [warnings_as_errors]}.

{deps, [
    {ndto, {git, "https://github.com/nomasystems/ndto.git", {branch, "main"}}}
]}.

{project_plugins, [
    erlfmt
]}.
{erlfmt, [write]}.

{alias, [
    {check, [
        {fmt, "--check"},
        dialyzer
    ]}
]}.
